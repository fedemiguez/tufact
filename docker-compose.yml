services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tufact
    ports:
      - "5432:5432"
    volumes:
      - tufact_pg:/var/lib/postgresql/data

volumes:
  tufact_pg:
